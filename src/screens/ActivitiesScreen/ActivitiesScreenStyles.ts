import {StyleSheet} from 'react-native';
import {colors} from '../../styles/colors';

export const styles = StyleSheet.create({
  ActivitiesScreen: {
    flex: 1,
    backgroundColor: colors.n6,
  },

  ActivitiesScreen__title: {
    color: colors.n4,
    fontSize: 24,
    fontWeight: '500',
    textAlign: 'center',
    marginBottom: 6,
  },
});
